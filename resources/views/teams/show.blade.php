<x-layout.layout>
    <div class="flex justify-between items-center">
        <x-header
            title="{{ $team->name }}"
            description="{{ $team->description }}"
        />
    </div>
</x-layout.layout>
